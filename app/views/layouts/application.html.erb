<!DOCTYPE html>
<html>
  <head>
    <title>GoodJobHuntingApp</title>
 
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css"> 

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="nav-bar">
    <% unless params[:controller] == "sessions" &&  params[:action] == "login" %>
        <% if logged_in? %>
    <div class = "ui clearing segment">
      <h4 class = "ui left floated header">
            Welcome <%= current_user.name %>
           <span class = "nav-link"> <%= link_to "Profile", user_path(current_user) %> </span>
       </h4>
      <h4 class = "ui right floated header">
            <%= link_to "Homepage", root_path %>
          <span class = "nav-link">   <%= link_to "Logout", logout_path, method: :delete %> </span>
      </h4>
      </div>
        <% else %>
        <div class = "ui clearing segment">
      <h4 class = "ui left floated header">
            <%= link_to "Homepage", root_path %>
      </h4>
      <h4 class = "ui right floated header">
          <span class = "nav-link">   <%= link_to "Login", login_path %> 
            <%= link_to "SignUp", signup_path %>
          </span>
      </h4>
      </div>
        <% end %>
    <% end %>
</div>
<div class="content">
    <%= yield %>
</div>
  </body>
</html>
